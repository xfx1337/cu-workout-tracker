# --- Mattermost (TiMe) ---
MM_URL=https://time.example.ru
MM_TOKEN=personal_access_token_бот_аккаунта
MM_TEAM=

# Адрес, по которому сервер Mattermost может достучаться до бота.
# Пусто => выбор делается реакциями-эмодзи, входящий порт не нужен.
# Задать => те же экраны отрисуются кнопками-attachments.
MM_PUBLIC_URL=
MM_LISTEN_PORT=8080

# Админы бота: корпоративная почта, через запятую.
# Почта, а не username: username в Mattermost человек может сменить сам.
ADMIN_EMAILS=olya@centraluniversity.ru

# --- База ---
DATABASE_URL=postgresql+asyncpg://workout:workout@127.0.0.1:5432/workout
# для локального прогона без Postgres:
# DATABASE_URL=sqlite+aiosqlite:///./workout.db

POSTGRES_USER=workout
POSTGRES_PASSWORD=workout
POSTGRES_DB=workout
POSTGRES_PORT=5432

# --- Время ---
TZ_NAME=Europe/Moscow

# --- Правила записи ---
DEFAULT_CAPACITY=25
REMINDER_MINUTES_BEFORE=30
CANCEL_DEADLINE_MINUTES=0
NO_SHOW_LIMIT=3
SILENCE_GRACE_HOURS=24
SELF_REPORTED_ABSENCE_COUNTS=false
SILENCE_COUNTS=true
